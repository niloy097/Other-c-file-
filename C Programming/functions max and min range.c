#include<stdio.h>
#include<limits.h>
int main()
{
    printf("The maximum value if INT = %d\n", INT_MAX);
     printf("the maximum value of int=%d\n",INT_MIN);
     printf("the minimum value of LONG =%ld\n",LONG_MIN);
    printf("the maximum value of LONG =%ld\n",LONG_MAX);
}
