#include<stdio.h>
void main()
{
    if(1==2 !=0)                   //Here,
                                        // 1 is not equal to 2, so the answer is false(0) and 0 is equal to 0, so the ans is false(0).
                                        // 2nd statement will be approved!!
        printf("Hi\n");
    else
        printf("Hello\n");

}
